export const appName = 'REST client';

export const layoutConfig = {
  headerHeight: 80,
  footerHeight: 60,
} as const;

export const paths = {
  main: '/',
  login: '/login',
  registration: '/registration',
  rest: '/rest',
  history: '/history',
  variables: '/variables',
} as const;
